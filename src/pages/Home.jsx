export default function Home() {
  return (
    <section className="card">
      <h1>Welcome</h1>
      <p>
        This React app demonstrates Router navigation, Redux state management,
        and modern CSS3 transitions. Use the links above to add employees and
        view the list.
      </p>
    </section>
  );
}